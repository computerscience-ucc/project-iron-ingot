# --- Sanity CMS (read by both the website AND the embedded Studio) ---
NEXT_PUBLIC_SANITY_PROJECT_ID=gjvp776o
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2023-10-01

# Optional. Required only if the dataset is private. Leave blank for the public `production` dataset.
SANITY_API_TOKEN=

# --- Google Gemini (powers the CSBot chat widget) ---
GEMINI_API_KEY=KEY_HERE
